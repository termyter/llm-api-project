# 🚀 Инструкция по запуску

## Виртуальное окружение

Проект использует виртуальное окружение Python для изоляции зависимостей.

### Активация виртуального окружения:

```bash
cd /Users/ivanemelanov/Xcode/llm-api-project
source venv/bin/activate
```

После активации вы увидите `(venv)` перед приглашением командной строки.

### Деактивация:

```bash
deactivate
```

---

## Настройка API ключей

1. Скопируйте `.env.example` в `.env`:
   ```bash
   cp .env.example .env
   ```

2. Откройте `.env` в текстовом редакторе и добавьте свои ключи:
   ```
   OPENAI_API_KEY=ваш_ключ_openai
   TELEGRAM_BOT_TOKEN=ваш_токен_бота
   ```

---

## Запуск

### CLI версия:
```bash
source venv/bin/activate
python3 main.py
```

### Telegram Bot:
```bash
source venv/bin/activate
python3 telegram_bot.py
```

---

## Получение токена Telegram бота

См. файл `TELEGRAM_SETUP.md`
